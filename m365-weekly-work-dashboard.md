---
name: m365-weekly-work-dashboard
description: >-
  사용자의 M365 주간 업무 활동을 상세하게 탐색할 수 있는 단일 HTML 대시보드를 생성한다.
  관리자용 Word 요약 보고서와 동일한 근거 데이터를 사용하되, 세부 활동, 산출물, 협업,
  의사결정과 후속 조치까지 제공한다. 이번 주 업무 대시보드, M365 활동 상세 HTML,
  주간 리포트 상세판을 요청할 때 사용한다.
---

# M365 Weekly Work Dashboard

## 목적

관리자용 Word 보고서는 핵심 성과 3~5개만 A4 1페이지 내외로 요약한다. 이 스킬은 같은 기준 기간과 같은 M365 근거 데이터로 개인 업무 추적용 상세 HTML 대시보드를 만든다.

## 필수 실행 규칙

- 기준 기간 기본값은 이번 주 월요일 00:00부터 금요일 23:59까지다. 금요일 전이라면 실행 시점까지만 사용한다.
- Outlook, Teams, OneDrive, SharePoint 및 접근 가능한 Planner, To Do, Loop 데이터를 폭넓게 조사한다.
- 생성, 수정, 발송, 회신, 공유, 검토, 승인, 의사결정, 발표, 완료 등 실질 활동만 포함한다.
- 단순 열람, 읽음, 자동 알림, 반복 활동과 중복 기록은 제외한다.
- 같은 업무의 메일, 채팅, 회의, 파일 활동은 하나의 업무 흐름으로 통합하고 근거를 연결한다.
- 데이터에 없는 성과, 기한, 진행률, 참석, 발언 또는 의사결정을 추측하지 않는다.

## 필수 HTML 템플릿

모든 대시보드는 스킬 패키지에 포함된 `dashboard-template.html`을 복사하여 생성한다. 새로운 HTML 구조를 임의로 작성해서는 안 된다.

템플릿에서 변경할 수 있는 값은 다음 플레이스홀더뿐이다.

- `{{DASHBOARD_TITLE}}`
- `{{AUTHOR_NAME}}`
- `{{PERIOD}}`
- `{{GENERATED_AT}}`
- `{{DASHBOARD_JSON}}`

생성 절차:

1. `dashboard-template.html`을 그대로 읽는다.
2. M365 활동을 아래 데이터 계약에 맞게 정규화한다.
3. 텍스트 플레이스홀더는 HTML 이스케이프된 값으로 치환한다.
4. `DASHBOARD_JSON`은 안전하게 직렬화한 JSON으로 치환한다. JSON 안의 `</script>`는 `<\/script>`로 이스케이프한다.
5. 템플릿의 요소 순서, CSS 클래스, CSS 변수, JavaScript 함수, 필터 구조를 변경하지 않는다.
6. 데이터가 없는 섹션을 삭제하지 않고 템플릿의 빈 상태 문구를 유지한다.
7. 생성 후 `{{` 또는 `}}` 플레이스홀더가 남아 있지 않은지 검사한다.

## 고정 디자인

템플릿은 SK 계열사의 공식 CI 페이지에 제시된 대표 계열 색상에서 영감을 받은 다음 팔레트를 고정 사용한다.

- SK Red: `#EA002C`
- SK Orange: `#F47725`
- SK Yellow: `#FBBC05`
- SK Teal: `#009A93`
- SK Blue: `#0072C6`
- SK Navy: `#0E306D`

SK 로고 또는 Happy Wing 이미지는 사용자가 승인된 브랜드 자산을 명시적으로 제공한 경우에만 사용한다. 템플릿은 SK 공식 문서나 정식 브랜드 산출물인 것처럼 표시하지 않고 색상에서만 영감을 받은 사내 업무 대시보드로 만든다.

다음 디자인 요소는 변경하지 않는다.

- SK Red에서 SK Orange로 이어지는 헤더 그라데이션
- 6개 KPI 카드와 고정된 색상 순서
- 흰색 카드, 18px 모서리, 얕은 SK Navy 계열 그림자
- SK Navy 섹션 제목 및 표 헤더
- 검색과 필터 도구 모음의 순서
- 데스크톱 6열, 태블릿 3열, 모바일 2열 KPI 배치
- 타이포그래피: `Aptos, Noto Sans KR, Segoe UI, sans-serif`

## 데이터 계약

`DASHBOARD_JSON`은 다음 최상위 구조를 사용한다.

```json
{
  "metrics": {
    "activities": 0,
    "completed": 0,
    "inProgress": 0,
    "followup": 0,
    "artifacts": 0,
    "collaboration": 0
  },
  "achievements": [],
  "distribution": { "projects": {} },
  "activities": [],
  "artifacts": [],
  "collaboration": [],
  "followups": [],
  "provenance": {
    "includedApps": [],
    "unavailableApps": [],
    "lastDataAt": ""
  }
}
```

각 `activities` 항목은 다음 필드를 사용한다.

```json
{
  "id": "고유 ID",
  "dateTime": "ISO 8601",
  "title": "행동과 결과가 드러나는 제목",
  "summary": "상세 활동",
  "outcome": "확인 가능한 결과",
  "project": "프로젝트 또는 기타",
  "status": "완료 | 진행 중 | 대기 | 후속 필요",
  "activityType": "생성 | 수정 | 회신 | 공유 | 회의 | 의사결정 | 검토 | 완료 | 기타",
  "sourceApp": "Outlook | Teams | OneDrive | SharePoint | Planner | To Do | Loop | 기타",
  "people": [],
  "artifact": "관련 산출물",
  "sourceUrl": "실제 https 원본 링크 또는 빈 문자열",
  "evidence": "원본에서 확인한 사실",
  "nextAction": "확인된 후속 조치 또는 빈 문자열",
  "dueDate": "확인된 기한 또는 빈 문자열"
}
```

## 집계 규칙

- 실질 활동: 중복 제거 후 `activities` 개수
- 완료: 상태가 완료인 활동 수
- 진행 중: 상태가 진행 중인 활동 수
- 후속 필요: 상태가 후속 필요인 활동 수
- 산출물: 생성 또는 실질 수정한 고유 파일과 문서 수
- 협업 업무: 둘 이상의 인물 또는 조직이 관련된 고유 업무 흐름 수
- 신뢰할 수 없는 수치는 0으로 꾸미지 말고 `확인 불가`로 표시한다.

## 고정 화면 구조

1. SK Red와 SK Orange 그라데이션 헤더
2. KPI 카드 6개
3. 핵심 성과와 프로젝트별 업무 분포
4. 상세 활동 탐색기
5. 산출물 및 문서
6. 협업 및 의사결정
7. 다음 주 후속 조치
8. 데이터 출처와 생성 기준
9. 보안 안내 푸터

## 상호작용

템플릿에 내장된 다음 기능을 모두 보존한다.

- 전체 검색
- 프로젝트, 상태, 앱, 활동 유형 필터
- 최신순, 오래된순, 프로젝트순 정렬
- 필터 초기화
- 현재 결과 수
- 인쇄
- CSV 내보내기
- 키보드 focus 상태
- 반응형 레이아웃
- `prefers-reduced-motion`

## 보안 및 개인정보

- 메일과 채팅 전체 본문을 노출하지 않고 업무 이해에 필요한 최소 요약과 근거만 표시한다.
- 자격 증명, 비밀키, 개인식별번호, 민감 정보를 출력하지 않는다.
- 다른 직원의 감정, 태도, 역량, 성과 또는 기여도를 평가하거나 추론하지 않는다.
- `sourceUrl`은 실제 `https:` 링크만 허용하며, 링크가 없으면 버튼을 만들지 않는다.
- 사용자 및 M365 문자열을 HTML 이스케이프한다.

## 저장과 메일

- HTML 파일명: `[주간업무대시보드]{연도}{월}_{주차}_{프로필이름}.html`
- OneDrive 저장 위치: `Documents/weekly_reports/details`
- 폴더가 없고 생성 도구가 있으면 폴더를 만든다.
- 관리자용 Word 보고서에는 `상세 업무 대시보드` OneDrive 링크를 추가한다.
- 발송 메일에는 Word와 HTML을 모두 첨부하고, 본문에 두 OneDrive 링크를 넣는다.
- 조직 정책상 HTML 첨부가 제한되면 HTML은 첨부하지 않고 OneDrive 링크만 넣는다.
- 실제로 저장하거나 발송하지 못한 경우 성공한 것으로 표현하지 않는다.

## 품질 점검

- [ ] `dashboard-template.html`을 사용했다.
- [ ] 플레이스홀더가 남아 있지 않다.
- [ ] 기준 기간과 작성자가 정확하다.
- [ ] KPI와 상세 데이터가 일치한다.
- [ ] 중복 활동이 제거되었다.
- [ ] 핵심 성과의 `activityIds`가 실제 활동 ID와 연결된다.
- [ ] 검색, 필터, 정렬, 인쇄, CSV가 작동한다.
- [ ] 실제 링크가 없는 항목에 가짜 링크가 없다.
- [ ] 외부 CDN, 원격 글꼴, 외부 JavaScript에 의존하지 않는다.
- [ ] 375px, 768px, 1440px에서 레이아웃이 유지된다.
- [ ] SK 색상, 섹션 순서, CSS 클래스와 컴포넌트 형태가 변경되지 않았다.

## 최종 응답

생성 후 채팅에는 기준 기간, 실질 활동 수, 핵심 성과 수, HTML 파일 링크, OneDrive 저장 결과, 메일 발송 결과, 접근하지 못한 앱만 간결하게 표시한다. 상세 활동 전체를 채팅에 다시 나열하지 않는다.
